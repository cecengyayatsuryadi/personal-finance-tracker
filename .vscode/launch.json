{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch Main",
            "program": "${workspaceFolder}/main.py"
        }
    ]
}